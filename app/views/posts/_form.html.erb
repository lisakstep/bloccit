 <%= simple_form_for [topic, post] do |f| %>

  <%= f.input :title, as: :string, class:'form-control', placeholder: "Enter post title" %>
  <%= f.input :body, as: :text, class:'form-control', placeholder: "Enter post body" %>  
  <%= f.submit "Save", class: 'btn btn-success' %>

<% end %>